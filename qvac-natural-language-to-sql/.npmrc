# @qvac/sdk pulls in optional Expo / react-native tooling (for its mobile
# targets) whose peer wants React 19. This desktop Electron example only uses
# React 18 for the renderer and never touches react-native, so we relax peer
# resolution to keep `npm install` clean. Safe: the conflicting peers are
# optional and unused here.
legacy-peer-deps=true
