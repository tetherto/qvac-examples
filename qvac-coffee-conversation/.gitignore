# Dependencies / build
node_modules/
dist/
*.log
.DS_Store

# QVAC model cache (downloaded at runtime)
models/
*.gguf

# Secrets - NEVER commit (wallet seeds, keys, real env)
.env
.env.*
!.env.example
.envBTC
.envUSDT*
*.seed.json
**/tether-wdk-seed.json
**/wallet-keys.json
.local-secrets-backup/
qvac-wallet/
qvac-wallet-phoenixd/
